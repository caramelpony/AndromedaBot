"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.TarFile = void 0;
const fs = require("fs");
class TarFile {
    read() {
        return new Promise((resolve, reject) => {
            fs.open(this.archiveFile, 'r', (err, fd) => {
                if (err) {
                    reject(err);
                }
                else {
                    const buf = Buffer.alloc(this.fileSize);
                    fs.read(fd, buf, 0, this.fileSize, this.offset, (err2, _, buffer) => {
                        if (err2) {
                            reject(err2);
                        }
                        else {
                            resolve(buffer);
                        }
                    });
                }
            });
        });
    }
}
exports.TarFile = TarFile;
//# sourceMappingURL=TarFile.js.map