import { GameObject } from './public/GameObject';
import { Region } from './Region';
export declare class ObjectResolver {
    private region?;
    private objectsInQueue;
    private queue;
    private maxConcurrency;
    private currentlyRunning;
    private onObjectResolveRan;
    constructor(region?: Region | undefined);
    resolveObjects(objects: GameObject[], forceResolve?: boolean, skipInventory?: boolean, log?: boolean): Promise<GameObject[]>;
    private scanObject;
    private run;
    private doResolve;
    shutdown(): void;
}
